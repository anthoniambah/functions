// A simple function that displays a simple message outside a function that says, "This is a text outside a function block".

function displayTextOutside(message) {
  return message;
}

displayTextOutside();
let message = "This is a text outside a function block";
console.log(message);
